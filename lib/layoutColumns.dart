import 'dependencies.dart';

Column columnMain() {
  return Column(
    children: <Widget>[
      
      CircleAvatar(
        radius: 75,
        backgroundColor: Avatar0,
        child: Image.asset(''),
      ),


    ],
  );
}

Column column0() {
  return Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[
          container1A(),
          // spacer(),
          container1B(),
        ],
      );
}